<div data-controller="modal">
  <div id="<%= modal_id %>" class="fixed inset-0 bg-black bg-opacity-70 hidden flex items-center justify-center modal transition-opacity duration-300">
    <div class="relative w-2/5 max-w-2xl p-8 bg-gray-800 text-white rounded-lg shadow-xl" style="min-width: 300px;">
      <button
        type="button"
        data-action="click->modal#close"
        class="absolute top-4 right-4 text-white text-2xl hover:text-gray-400 transition duration-200"
      >
        ×
      </button>
      <div class="modal-content">
        <%= yield %>
      </div>
    </div>
  </div>
</div>
