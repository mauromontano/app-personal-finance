<div data-controller="modal">
  <div id="<%= modal_id %>" class="fixed inset-0 bg-black bg-opacity-70 hidden flex items-center justify-center modal transition-opacity duration-300">
    <div class="relative w-2/5 max-w-2xl p-8 bg-gray-800 text-white rounded-modal shadow-xl" style="min-width: 300px;">
      <button
        type="button"
        data-action="click->modal#close"
        class="absolute -top-2 -right-2 flex items-center justify-center w-8 h-8 bg-gray-700 text-gray-300 rounded-full hover:bg-gray-600 hover:text-white transition-colors duration-200"
      >
        ×
      </button>
      <div class="modal-content">
        <%= yield %>
      </div>
    </div>
  </div>
</div>
