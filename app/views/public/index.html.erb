<div class="min-h-screen flex flex-col justify-between bg-gradient-to-b from-blue-900 to-blue-700 text-white">

  <div class="flex flex-col items-center justify-center flex-grow text-center p-6">
    <h1 class="text-4xl font-bold mb-4">Maneja todas tus finanzas de la manera mas simple!</h1>
    <p class="text-lg mb-6">Por favor, inicia sesión para acceder a la aplicación.</p>

    <button
      data-controller="modal"
      data-action="click->modal#open"
      data-modal-target="loginModal"
      class="px-6 py-3 bg-white text-blue-700 font-semibold rounded-lg shadow-md hover:bg-gray-200 transition"
    >
      Iniciar Sesión
    </button>
  </div>

  <%= render "modal/modal", modal_id: "loginModal" do %>
    <%= render "modal/login_form", resource: resource, resource_name: resource_name %>
  <% end %>
</div>
